{{- if and .Values.subscription.enabled (not .Values.ingress.enabled) }}
{{- fail "subscription.enabled=true requires ingress.enabled=true with subscription.ingress.hosts[] configured." }}
{{- end }}
