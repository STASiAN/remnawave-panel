{{- if and .Values.gateway.enabled .Values.subscription.enabled (gt (len .Values.subscription.gateway.hostnames) 0) -}}
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: {{ include "remnawave-panel.subscription.fullname" . }}
  labels:
    {{- include "remnawave-panel.subscription.labels" . | nindent 4 }}
  {{- with .Values.subscription.gateway.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  parentRefs:
    {{- if .Values.subscription.gateway.parentRefs }}
    {{- range .Values.subscription.gateway.parentRefs }}
    - {{- toYaml . | nindent 6 }}
    {{- end }}
    {{- else }}
    {{- if .Values.gateway.gatewayName }}
    - name: {{ .Values.gateway.gatewayName }}
    {{- else if .Values.gateway.createGateway }}
    - name: {{ include "remnawave-panel.fullname" . }}
      {{- if eq (include "remnawave-panel.subscription.httpsRedirect.render" .) "true" }}
      {{- /* the redirect route owns the http listener; serve on https only */}}
      sectionName: https
      {{- end }}
    {{- end }}
    {{- range .Values.gateway.parentRefs }}
    - {{- toYaml . | nindent 6 }}
    {{- end }}
    {{- end }}
  hostnames:
    {{- range .Values.subscription.gateway.hostnames }}
    - {{ . | quote }}
    {{- end }}
  rules:
    - matches:
        - path:
            type: PathPrefix
            value: /
      backendRefs:
        - name: {{ include "remnawave-panel.subscription.fullname" . }}
          port: {{ .Values.subscription.port }}
{{- end }}
